{
  "name": "pla-forge-site",
  "private": true,
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "dev": "npm run build && wrangler pages dev",
    "deploy:test": "npm run build && wrangler pages deploy --project-name pla-forge-test --branch test"
  },
  "devDependencies": { "wrangler": "4.125.0" }
}
